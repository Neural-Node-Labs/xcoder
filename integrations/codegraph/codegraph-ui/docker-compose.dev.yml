services:
  codegraph-ui:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "5173:5173"
    volumes:
      - ./src:/app/src
      - ./index.html:/app/index.html
    environment:
      - CHOKIDAR_USEPOLLING=true
